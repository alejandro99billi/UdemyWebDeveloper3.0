const n  = "hola mundo"
const m  = "hola"
console.log(typeof n)
 if(typeof n == "string" && typeof m == "string"){
    //inidicar por cada variable el valor que debe tener
 } else {
    console.warn('datos invalidos')
   
 }